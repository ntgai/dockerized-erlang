[%% Kernel/logger
 {kernel, [{logger,[{handler,default,logger_std_h,#{}}]}
          %%,{logger_level,info}
          ]},
 {myapp, [{client_id, "myapp"}, {host, "localhost"}]},
 {cowboy_conf, [{port, 8080}]}
].
